import { Typography, Box } from '@mui/material';

export default function LoginPage() {
  return (
    <Box display="flex" justifyContent="center" alignItems="center" minHeight="100vh">
      <Typography variant="h4">Página de Login</Typography>
    </Box>
  );
}
